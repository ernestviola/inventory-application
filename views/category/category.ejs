<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= category.name %></title>
  </head>
  <body>
    <h1><%= category.name %></h1>
    <h2>Items</h2>
    New Item
    <%- include('../components/itemForm',{url: '/item/new'}) %>

    <% if (items) { %>
      <ul>
      <% items.forEach( item => { %>
        <li>
          <span><%= item.name %></span>
          <form action='/item/edit' method='get'><button type='submit'>Edit</button></form>
          <form action='/item/delete' method='post'><button type='submit'>Delete</button></form>
        </li>
      <% }) %>
      </ul>
    <% } %>
  </body>
</html>
